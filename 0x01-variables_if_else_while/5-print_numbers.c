#include <stdio.h>
/**
 * main - To print if the number is zero, positive or negative
 *
 * Description: use the main function to return the values
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
{
	printf("%i", i);
}
printf("\n");
return (0);
